The runtest.sh script has been tested successfully with test1.lst, test2.lst, test3.lst, test4.lst
on the following platforms:

Linux Ubuntu with gcc, 32 and 64 bits

Linux Ubuntu with clang, 32 and 64 bits

Windows with MS Visual Studio 2019, 32 and 64 bit (Using Cygwin bash)

Windows subsystem for Linux (WSL) with Ubuntu and gcc, clang, intel ICC, and intel ICPX compilers 64 bit

Mingw-w64 with gcc, 64 bit

Cygwin64 with gcc and Clang, 64 bit

Windows with Intel compiler, ICL and ICPX, 64 bit
